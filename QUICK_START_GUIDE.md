# 🚀 دليل التشغيل السريع

## ✅ تم إصلاح جميع المشاكل!

النظام الآن جاهز للعمل بشكل صحيح.

---

## 📋 قبل التشغيل:

### 1. تأكد من التثبيت
```bash
# إذا لم تقم بالتثبيت بعد
install.bat
```

### 2. فحص الإعدادات (اختياري)
ملف: `config/settings.json`
- ✅ جميع الإعدادات محسّنة ومناسبة
- يمكنك تخصيصها حسب الحاجة

---

## 🎯 التشغيل:

### الطريقة الموصى بها:
```bash
start.bat
```

### ما يحدث عند التشغيل:
```
1. ✅ فحص صلاحيات المسؤول
2. ✅ تهيئة نظام الحماية
3. ✅ نشر 50 ملف فخ
4. ✅ اكتشاف مجلدات المستخدم تلقائياً
5. ✅ فحص أقراص USB
6. ✅ بدء المراقبة الشاملة
7. ✅ تشغيل Dashboard
```

---

## 📊 المجلدات المحمية تلقائياً:

### مجلدات شخصية:
- ✅ `C:\Users\{username}\Documents`
- ✅ `C:\Users\{username}\Desktop`
- ✅ `C:\Users\{username}\Pictures`
- ✅ `C:\Users\{username}\Downloads`
- ✅ `C:\Users\{username}\Videos`

### مجلدات إضافية:
- ✅ المجلدات المحددة في `settings.json`
- ✅ أقراص USB المتصلة
- ✅ الأقراص الخارجية

### المجلدات المستثناة (لا تُراقب):
- ❌ مجلد المشروع نفسه
- ❌ ملفات قاعدة البيانات
- ❌ النسخ الاحتياطية
- ❌ السجلات

---

## 🌐 الوصول للوحة التحكم:

### افتح المتصفح:
```
http://localhost:8000
```

### الميزات المتاحة:
- 📊 **Dashboard**: الإحصائيات الحية
- 🚨 **Alerts**: التنبيهات
- 🔍 **Events**: سجل الأحداث
- 🛡️ **Protection**: النسخ الاحتياطية
- 💾 **Drives**: الأقراص المراقبة

---

## 🔍 مراقبة السجلات:

### السجلات الحية:
ستظهر في نافذة الأوامر:
```
INFO - File protection system initialized ✅
INFO - Deployed 50 decoy files ✅
INFO - Auto-added user folder: ... ✅
INFO - Starting file system monitoring ✅
INFO - Ransomware Detection Engine is running ✅
```

### السجلات المحفوظة:
```
logs/ransomware_defense.log
```

---

## ⚠️ التنبيهات الطبيعية:

### هذه رسائل عادية (ليست أخطاء):
```
✅ "Auto-added user folder" - إضافة مجلدات تلقائياً
✅ "Backup created" - نسخ احتياطي تم إنشاؤه
✅ "Monitoring: ..." - المراقبة نشطة
```

### رسائل يجب الانتباه لها:
```
⚠️ "Decoy file compromised" - ملف فخ تم العبث به!
⚠️ "High entropy detected" - تشفير محتمل!
🔴 "Ransomware detected" - هجوم مكتشف!
```

---

## 🛑 إيقاف النظام:

### الطريقة الصحيحة:
```
في نافذة الأوامر: اضغط Ctrl+C
انتظر رسالة "Shutting down..."
```

### لا تغلق النافذة مباشرة!
- ⚠️ قد يتسبب في تلف قاعدة البيانات
- ✅ استخدم دائماً Ctrl+C

---

## 🧪 اختبار النظام:

### اختبار بسيط:
1. افتح ملف نصي في Desktop
2. اكتب شيئاً واحفظ
3. تحقق من السجلات
4. يجب أن ترى "Backup created"

### اختبار متقدم:
```bash
python test_system.py
```

---

## 📈 API Endpoints:

### الحالة:
```bash
GET http://localhost:8000/api/status
```

### الإحصائيات:
```bash
GET http://localhost:8000/api/stats
```

### النسخ الاحتياطية:
```bash
GET http://localhost:8000/api/protection/stats
```

### الأقراص:
```bash
GET http://localhost:8000/api/drives/list
```

---

## 🔧 حل المشاكل:

### المشكلة: النظام لا يبدأ
**الحل:**
```bash
# تأكد من التثبيت
install.bat

# جرب مرة أخرى
start.bat
```

### المشكلة: أخطاء في قاعدة البيانات
**الحل:**
```bash
# إصلاح قاعدة البيانات
fix_database.bat
```

### المشكلة: كشف خاطئ
**الحل:**
- ✅ تم إصلاحها في التحديث الأخير!
- النظام الآن لا يكتشف نفسه

---

## 💡 نصائح الاستخدام:

### 1. صلاحيات المسؤول
- ✅ مطلوبة للاحتواء الكامل
- الكشف يعمل بدونها، لكن الاحتواء محدود

### 2. مجلدات الاختبار
- استخدم مجلدات منفصلة للتجارب
- لا تختبر على ملفات مهمة

### 3. النسخ الاحتياطي
- يحفظ آخر 5 نسخ فقط
- للملفات المهمة: احتفظ بنسخ إضافية خارجية

### 4. الأداء
- النظام خفيف ولا يستهلك موارد كثيرة
- يمكن تشغيله في الخلفية دائماً

---

## 🎯 الملخص:

### للتشغيل العادي:
```bash
1. start.bat
2. افتح http://localhost:8000
3. راقب لوحة التحكم
```

### للإيقاف:
```bash
Ctrl+C في النافذة
```

### للإصلاح:
```bash
fix_database.bat
```

---

## ✨ مميزات محسّنة:

- ✅ لا حلقات لانهائية
- ✅ لا أخطاء Permission denied
- ✅ كشف دقيق
- ✅ أداء ممتاز
- ✅ استقرار كامل

**النظام جاهز للعمل! 🚀**

---

## 📞 المساعدة:

راجع:
- `DATABASE_FIX_GUIDE.md` - إصلاح قاعدة البيانات
- `FIXES_APPLIED.md` - الإصلاحات المطبقة
- `README.md` - الدليل الشامل

# 🛡️ شرح شامل ومفصل لنظام كشف الفدية (Ransomware Detection System)

## 📑 جدول المحتويات
1. [نظرة عامة على النظام](#نظرة-عامة)
2. [البنية المعمارية](#البنية-المعمارية)
3. [المكونات الرئيسية](#المكونات-الرئيسية)
4. [آلية العمل](#آلية-العمل)
5. [تفاصيل الكود](#تفاصيل-الكود)
6. [قاعدة البيانات](#قاعدة-البيانات)
7. [واجهة المستخدم](#واجهة-المستخدم)

---

## 🎯 نظرة عامة على النظام

### ما هو هذا النظام؟
نظام احترافي لكشف واحتواء هجمات الفدية (Ransomware) يعمل على نظام Windows. يستخدم تقنيات متقدمة مثل:
- **مراقبة سلامة الملفات** (File Integrity Monitoring)
- **ملفات الفخ الذكية** (Honeypot/Decoy Files)
- **تحليل الإنتروبيا** (Entropy Analysis)
- **الاحتواء التلقائي** (Automated Containment)

### الهدف الرئيسي
اكتشاف محاولات تشفير الملفات بواسطة برامج الفدية في الوقت الفعلي واتخاذ إجراءات احتواء فورية.

---

## 🏗️ البنية المعمارية (Architecture)

### الطبقات الأساسية

```
┌─────────────────────────────────────────────────┐
│           واجهة المستخدم (React)                 │
│      Dashboard + Real-time Monitoring           │
└──────────────────┬──────────────────────────────┘
                   │ WebSocket + REST API
┌──────────────────▼──────────────────────────────┐
│          طبقة API (FastAPI Backend)             │
│   - REST Endpoints  - WebSocket Handler         │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│              المكونات الأساسية                  │
│  ┌────────────┬──────────┬──────────────────┐  │
│  │ File       │ Decoy    │ Ransomware       │  │
│  │ Monitor    │ Manager  │ Detector         │  │
│  └────────────┴──────────┴──────────────────┘  │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│         محرك الاحتواء (Containment)             │
│  - Network Isolation - Process Kill             │
│  - Drive Disconnect  - System Lock              │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│         قاعدة البيانات (SQLite)                 │
│  Events | Incidents | Alerts | Decoys           │
└─────────────────────────────────────────────────┘
```

---

## 🔧 المكونات الرئيسية

### 1️⃣ File Monitor (مراقب الملفات)
**الملف:** `backend/core/file_monitor.py`

#### الوظائف:
- **مراقبة نظام الملفات**: يستخدم مكتبة `watchdog` لمراقبة التغييرات
- **حساب Hash**: SHA-256 لكل ملف لتتبع التعديلات
- **تحليل الإنتروبيا**: قياس عشوائية البيانات (القيم العالية تدل على التشفير)

#### كيف يعمل:
1. يراقب المجلدات المحددة في الإعدادات
2. عند أي تغيير (تعديل، إنشاء، حذف، نقل):
   - يحسب hash الملف
   - يحسب entropy (الإنتروبيا)
   - يقارن مع القيم السابقة
   - يرسل البيانات للكاشف

#### مثال على حساب الإنتروبيا:
```python
def calculate_entropy(file_path):
    # قراءة أول 1MB من الملف
    data = read_file(file_path, 1024*1024)
    
    # حساب تكرار كل byte (0-255)
    frequencies = [0] * 256
    for byte in data:
        frequencies[byte] += 1
    
    # حساب Shannon Entropy
    entropy = 0
    for freq in frequencies:
        if freq > 0:
            probability = freq / len(data)
            entropy -= probability * log2(probability)
    
    # entropy > 7.0 = ملف مشفر محتمل
    return entropy
```

